# eslint-config-next pulls its plugins transitively. pnpm's strict node_modules
# hides them from ESLint's resolver, so hoist the eslint tree specifically
# rather than disabling strictness everywhere.
public-hoist-pattern[]=*eslint*
